# Implementation and verification ledger

This ledger preserves the complete v0.2 requirements while implementation proceeds. Pending items are not advertised as supported. No milestone or coverage threshold replaces the full requirement scope.

| Requirement | Implementation and authoritative verification | State |
|---|---|---|
| §1–2 harness boundaries, strict results and exit codes | `domain/gate.rs`, gate truth-table tests and CLI 0/1/2 scenarios | Core verified; delivery audit pending |
| §3.1 source declarations, matching dependencies, naming, parameterization, comments, line endings, commits, imports and module boundaries | Syntax rules, Maven effective-model/tree pairing, marker retention, compiled undeclared dependency usage and module dependency directions; built-in rules carry validated references to the external standards archive, whose schema validates source provenance, taxonomy, lifecycle status/outcome semantics, and rule mappings | Partial: additional ecosystems and access boundaries remain |
| §3.2 build commands, compilation, tests, static analysis, coverage, compatibility | Bounded command runner; version probes; validated baseline exit codes, tool identities and inputs; JUnit, Checkstyle, SpotBugs, PMD, SARIF, LCOV, Cobertura, JaCoCo, native coverage.py and generic JSON parsers; paired Java JAR builds and digest-pinned japicmp | Java compatibility, SARIF/Clippy and coverage-producer interoperability locally verified below; final requirements audit remains |
| §3.3–3.4 profiles, task contracts, repair feedback and manual evidence | `config/plan.rs`, selected-policy task planning, retained acceptance descriptions/conditions, task/profile CLI scenarios and signed external manual decisions | Task deletion/corruption cannot remove the selected plan; manual snapshot/policy/task binding, authorization, expiry, revocation and revalidation are implemented; broader trust audit remains |
| §3.5 commit, staged, worktree and path snapshots; all increment modes | Snapshot/baseline CLI fixtures; report-gate tests for changed lines, multiplicity, renamed files and affected unmodified sources; coverage omission/branch repair CLI loop | Core modes verified; project impact and full evidence lifecycle audit remain |
| §4–5 syntax/project/tool adapter ownership and multiple languages | Six syntax adapters; Maven facts and Python installation/metadata facts; language-plus-module test routing | Maven/Python pairing implemented; real-repository second-ecosystem audit and further semantics pending |
| §6.1–6.2 rule packages and custom DSL | Embedded packages, snapshot-bound custom loading, source/definition evidence, rule/command DAG, Maven/Python dependency assertions and authenticated provenance facts | Syntax/Git/file DSL, two-ecosystem pairing and signed run replay implemented; broader semantics remain |
| §6.3 applicability, execution and completeness | Pure domain tests; CLI 0/1/2 assertions, including missing required tool and stale report | Core verified; final audit pending |
| §6.4 declaration scopes, trailers and external source evidence | Explicit marker fields; signed complete-comparison replay; Git parent/entity association; mixed human/agent, staged, retention, merge, expiry and tampering fixtures | Agent-run provenance, AI-only scope and commit-to-entity trailer association implemented within documented limits; real-harness audit remains |
| §6.5 init, repeat safety, discovery and candidate configuration | Bounded nested discovery, source/manifest language routing, relevant packages/capabilities/gaps, marker options, command/report suggestions and atomic candidate creation; optional command adoption | Implemented for documented conventions; native tool semantics still require execution and team-selected policy |
| §6.6 source mapping, trusted policy and changed verification assets | CommonMark section digests and bound source reviews; one resolved policy commit for configuration, custom rules and tasks; candidate content/mode comparison and policy-tampering CLI fixtures | Review and task-selection contracts locally verified; comprehensive trust deployment and verification-asset audit remain |
| §7 all CLI commands and MR providers | `--mr`, bounded GitHub/GitLab HTTP adapters, local merge-base resolution and `tests/merge_request.rs` | MR implementation added; verification checkpoint below, live-service audit pending |
| §8 JSON/table/Markdown, fingerprints, executions and snapshot binding | Resolved policy commit and pinned rechecks; retained task conditions in JSON/table/Markdown; durable artifacts, actual tool versions, executable/lockfile identities, per-command/baseline guards and retained evidence after source invalidation | Core evidence implemented; comprehensive lifecycle/environment audit remains |
| §9.1 acceptance scenarios | Requirement-specific unit/integration suites | Pending |
| §9.2 pilot measurement and actual repair loop | [Pilot protocol](pilot.md) specifies selection, baseline attribution, immutable cases, reviewer classifications, repair budgets and denominators | Protocol prepared; team-selected repository, thresholds and measured acceptance remain pending |
| §10 rollout and stable extensibility | Complete implementation and compatibility fixtures | Pending |
| Reference-equivalent quality YAML | `code_quality.yml`, Rust PR matrix, ≥90% coverage gate, Miri/ASan, native Windows/macOS, packaging; local `qualitygate.yaml`; executable owner graph and Markdown anchors | All 18 CI jobs passed for `7d6fb2a`; the current 20-job revision requires its own CI results |

Reference inspected: `/opt/workspace/relay-knowledge`, including `Cargo.toml`, `.github/workflows/code_quality.yml`, `.github/workflows/pr-checks.yml`, and its architecture constraints. The reference has unrelated local changes and is read-only for this task. Its available committed graph is pinned to `a6a0c8a9ed7518534e1fd0e49f079b73d179764b`; its newer indexing task is retrying, so current workflow details are checked directly against files.

## Verification checkpoint

The local verification checkpoint on 2026-09-08 passed 52 library tests, nine core CLI integration tests, 13 custom-rule CLI integration tests, two documentation/architecture tests and one deterministic benchmark (77 tests total). Formatting, `cargo check --locked --all-targets --all-features`, and `cargo clippy --locked --all-targets --all-features -- -D warnings` also passed.

`cargo llvm-cov --locked --all-targets --all-features --summary-only --fail-under-lines 90` passed at 91.93% Rust line coverage (4,227 lines, 341 missed), improving on the earlier 82.06% checkpoint. Coverage includes the custom DSL, source/definition trust checks, report-scope invariants, coverage inventory/branch handling and command precondition/timeout scenarios. These results verify the implemented scope above; the remaining requirements and CI-only gates still need verification.

Self-hosted `cargo run --locked -- check --worktree --profile full --output-dir target/self-check --format json` completed all seven configured checks with no violations. Its immutable input digest was `sha256:4b0ae7231a61278f63886c1233aa1bf4419b291b853e0a535ae20c99ab312d32`, based on commit `e805626d33c438cd5e5157abbcd619f8be567041` plus the then-current worktree. Local evidence is in `target/self-check/run-wDL9nA/report.json` and adjacent logs. That checkpoint preceded the final rule-revision/protocol separation and permission-preserving configuration write, which were subsequently validated by the complete 77-test coverage run.

`cargo package --locked --allow-dirty` also passed, including Cargo's package compilation verification. The archive contains all nine embedded rule YAML files. These are local checkpoint results; Miri, ASan and native Windows/macOS jobs still require authoritative CI or platform execution.

The initial remote workflow failed GitHub's YAML validation before any job started: the unquoted Miri command ended with `domain::`. The command is now quoted, and `documentation_yaml_contracts_are_parseable` checks repository YAML files locally. This adds one documentation test beyond the 77-test coverage checkpoint.

[CI run 34214318949](https://github.com/coolplayagent/qualitygate-cli/actions/runs/34214318949), for commit `528ba0f4c94defc8786e4ca0c8229e32c9288e82`, passed 14 jobs including coverage, Miri, AddressSanitizer, macOS and packaging. Windows failed the path-confinement regression: OS path parsing consumed the backslash in user input `x\y` before validation. User policy paths now reject raw backslashes before normalization; paths obtained from the OS use a separate confined normalization entry point. The `.git` exclusion is case-insensitive. Temporary CLI Git fixtures disable automatic newline conversion to preserve the exact bytes under test. The new revision still requires its own Windows CI result.

The MR implementation checkpoint passed all 92 tests: 60 library, nine core CLI, 13 custom-rule CLI, six MR CLI, three quality and one benchmark test. Formatting, compilation and Clippy with warnings denied passed. The complete `cargo llvm-cov --locked --offline --all-targets --all-features --summary-only --fail-under-lines 90` run passed at **92.50% line coverage** (4,639 lines, 348 missed); its local output is `target/verification-mr-coverage.txt`. `cargo package --locked --offline --allow-dirty` also passed archive compilation verification; local output is in `target/verification-mr-package.txt`.

MR verification uses real temporary Git histories and bounded local HTTP provider fixtures. It covers GitHub/GitLab metadata, current target branch resolution, actual missing-object fetching, checkout/index/ref preservation, comparison movement, provider errors, stale metadata, shallow history, mutually exclusive selectors, response-size limits, HTTP deadlines, redirect refusal and host-bound credential selection. The MR suite is included in both the CI integration gate and self-hosted `qualitygate.yaml`. These controlled fixtures do not claim live-service or complete requirements acceptance.

[CI run 34222867719](https://github.com/coolplayagent/qualitygate-cli/actions/runs/34222867719), for `2a346eb320d68322ce5c4680845cc80e25ea9fe1`, passed 14 jobs including Miri, AddressSanitizer, macOS, coverage and packaging. The Windows path regression passed. Its remaining failure was a provider-fixture connection closed before the request completed. The fixture now explicitly restores blocking mode on accepted sockets: [Winsock accepts connections with the listener's properties](https://learn.microsoft.com/en-us/windows/win32/api/winsock2/nf-winsock2-accept). A delayed-request regression exercises this condition. Native Windows confirmation still requires the new commit's CI run.

Execution integrity now has a dedicated `tests/execution.rs` suite, included in CI and the local policy. It checks permanent invalidation after input mutation, restoration attempts, baseline corruption, declared versus unexpected exit codes, inconsistent findings exits, actual version probes and their failures, missing tool inputs, tool replacement, signal termination, task-specific expected exits, case-distinct artifact identities and retaining completed evidence when final source revalidation fails. See [the report contract](reports.md) for the migration from versionless report checks and explicit integrity limits.

The execution-evidence checkpoint passed **111 tests**: 65 library, nine core CLI, 13 custom-rule CLI, 13 execution CLI, seven MR CLI, three quality and one benchmark. Formatting, `cargo check --locked --offline --all-targets --all-features` and Clippy with warnings denied passed. The complete coverage run passed at **92.98% lines** (5,240 lines, 368 missed); output is in `target/verification-lifecycle-coverage.txt`. The first coverage run exposed a second HTTP-fixture race: responses lacked `Connection: close` while the server closed every connection. The fixture now declares that lifecycle consistently; the unchanged full suite then passed under coverage.

Self-hosted `target/debug/qualitygate check --worktree --profile full --output-dir target/self-lifecycle --format json` passed all seven configured checks. Every command recorded actual tool versions and verified its inputs before and after execution. Evidence is in `target/self-lifecycle/run-tsEHrz/report.json`, bound to `sha256:e42e7b8e51794422b0916384cfc60389f17bb4c2d59cf4e226b37e23304f8810` (commit `2a346eb320d68322ce5c4680845cc80e25ea9fe1` plus the then-current worktree). That self-hosted run preceded the fixture-only connection-header repair, which was subsequently covered by the complete 111-test coverage run.

`cargo package --locked --offline --allow-dirty` passed for the execution-evidence checkpoint, including compilation of the packaged source; local output is `target/verification-lifecycle-package.txt`.

## Maven project verification

The Maven/DAG revision passed **118 ordinary tests** (70 library, nine core CLI, 14 custom-rule CLI, 14 execution CLI, seven MR CLI, three quality and one benchmark). Formatting, compilation and Clippy with warnings denied passed. The complete coverage run passed at **93.05% lines** (5,753 lines, 400 missed), with output in `target/verification-project-coverage.txt`. The live Maven test runs separately from these toolchain-free suites.

[CI run 34227403351](https://github.com/coolplayagent/qualitygate-cli/actions/runs/34227403351), for `a7f9550515f2d98ce4ad57e86e1a277d1d8081a2`, completed successfully in all 15 jobs, including Windows, macOS, Miri, AddressSanitizer, coverage and packaging. This verifies the earlier execution-evidence checkpoint.

The Maven pairing checkpoint used a real Maven 3.9.16 installation (archive SHA-512 checked against Apache's published checksum), JDK 21.0.11, Help Plugin 3.5.1 and Dependency Plugin 3.8.1. `tests/maven.rs` passed its five-run resolution/deletion/repair/marker-retention/runtime-scope sequence, using isolated temporary Git and Maven repositories. The final revision passed in 147.40 seconds; output is `target/verification-live-maven-final.txt`. The dedicated CI job still needs remote confirmation. This fixture is not a §9.2 pilot.

Self-hosted `target/debug/qualitygate check --worktree --profile full --output-dir target/self-project --format json` passed all seven checks. Evidence is `target/self-project/run-3PCC19/report.json`, bound to `sha256:dd3c55b46457085fbe6de9a3d9847e4a0bea20ea054ef8883702cbd48d0d7f3d` (commit `a7f9550` plus the implemented worktree, before this evidence note). `cargo package --locked --offline --allow-dirty` passed archive compilation verification with 103 packaged files; output is `target/verification-project-package.txt`.

The module-boundary revision passed **124 ordinary tests** (76 library, nine core CLI, 14 custom-rule CLI, 14 execution CLI, seven MR CLI, three quality and one benchmark). Compilation, formatting and Clippy passed. Coverage passed at **93.20% lines** (5,928 lines, 403 missed); output is `target/verification-boundary-coverage.txt`. The first live reactor run passed in 151.52 seconds (`target/verification-live-boundary.txt`), checking a forbidden compiled dependency, incomplete validation after deleting only the declaration, and a passing caller/dependency repair.

[CI run 34231256430](https://github.com/coolplayagent/qualitygate-cli/actions/runs/34231256430), for `663ee19c63fdbada983664b7763539b889fe631d`, passed 14 of 16 jobs, including live Maven, coverage, Miri, AddressSanitizer and packaging. Windows exposed a same-content replacement that timestamps did not distinguish. The guard now also uses Windows file IDs, with an added preserved-mtime replacement regression. macOS exposed a physical-versus-logical temporary path mismatch in Maven outputs. Materialized workspaces now expose a canonical physical path while retaining cleanup ownership; a symlink-parent regression covers the distinction. The new native CI run must confirm both fixes.

## Module boundary follow-up evidence

Self-hosted `target/debug/qualitygate check --worktree --profile full --output-dir target/self-boundary --format json` passed all seven checks. Evidence is `target/self-boundary/run-tSJdBW/report.json`, bound to `sha256:859c83771cd16d904978a0735f4a9e315c423bc95ce7b9bad669f997d62b685e` (commit `663ee19` plus this implementation, before this evidence note). `cargo package --locked --offline --allow-dirty` passed archive compilation verification with 107 files; output is `target/verification-boundary-package.txt`.

[CI run 34297582658](https://github.com/coolplayagent/qualitygate-cli/actions/runs/34297582658), for `a89d904d3beba455e3463f9afb2ca3f08501fd23`, completed successfully in all 16 jobs, including native Windows/macOS, both live Maven tests, Miri, AddressSanitizer, coverage and packaging. This confirms both native fixes. A separate local combined Maven rerun timed out while downloading a plugin dependency (`target/verification-live-boundaries-final.txt`); that local run is not recorded as passing.

## Compiled dependency usage verification

The usage revision passed **129 ordinary tests** (81 library, nine core CLI, 14 custom-rule CLI, 14 execution CLI, seven MR CLI, three quality and one benchmark). Formatting, compilation and Clippy with warnings denied passed. The final complete coverage run passed at **93.42% lines** (6,320 lines, 416 missed); output is `target/verification-usage-coverage-final.txt`. Three live Maven tests remain explicitly separate from the ordinary suite.

`used-undeclared` consumes pinned Maven bytecode analysis from the same successful producer as the effective model and resolved tree. It checks source compilation counts and rejects absent, skipped, contradictory, filtered and unrecognized evidence. Parser/rule/config fixtures cover missing producers, foreign snapshots, undeclared coordinates, classifiers, separate main/test inventories, suppression configuration and command property overrides. The dedicated Maven job now includes `real_used_transitive_dependency_requires_direct_declaration`: compiled transitive Hamcrest usage fails, a POM suppression remains incomplete, a direct declaration repairs the violation, and removing the provider blocks validation. The final four-run live fixture passed in **141.17 seconds**, using Maven 3.9.16/JDK 21.0.11 and isolated Git/artifact repositories; output is `target/verification-live-usage-sealed.txt`.

Self-hosted `target/debug/qualitygate check --worktree --profile full --output-dir target/self-usage --format json` passed all seven checks. Evidence is `target/self-usage/run-4VvPbp/report.json`, bound to `sha256:8765eec2131c2ac80819e07538ef817c3cd8ff7bccb0c562e92ea01526af19b3` (commit `a89d904` plus this implementation, before this evidence note). `cargo package --locked --offline --allow-dirty` passed archive compilation verification with 110 files; output is `target/verification-usage-package.txt`. The new revision still requires its own remote CI confirmation.

## Maven usage follow-up evidence

[CI run 34303317295](https://github.com/coolplayagent/qualitygate-cli/actions/runs/34303317295), for `a6e5bd923a853b824302f5114cbd5dca7c040077`, completed successfully in all 16 jobs, including the three live Maven tests, native Windows/macOS, Miri, AddressSanitizer, coverage and packaging.

## Python project verification

The Python revision passed **137 ordinary tests** (89 library, nine core CLI, 14 custom-rule CLI, 14 execution CLI, seven MR CLI, three quality and one benchmark). Formatting, compilation and Clippy with warnings denied passed. Full coverage passed at **92.69% lines** (7,031 lines, 514 missed); output is `target/verification-python-coverage-safe.txt`. Three Maven tests and one Python test remain explicit live-tool CI checks, separate from the ordinary counts.

Python facts now cross-check static PEP 621 declarations, pip report version 1 and actual installed Core Metadata. The parser validates version constraints, environment markers, extras propagation, local project origin and complete dependency closure. Python assertions normalize distribution names and distinguish direct declarations from transitive availability. Rust fixtures exercise mixed-language ownership and preserve Maven's existing configuration form. The final live Python five-run sequence passed in **265.48 seconds** (`target/verification-live-python-safe.txt`), checking installation/test success, manifest-only dependency deletion, failed assertions, code repair and declaration removal. This run includes the marker-context refinements, uncached installation and a repository-local `pip.py` regression; `-E -P` protects installer and version probes from Python environment overrides and current-directory shadowing.

Self-hosted `target/debug/qualitygate check --worktree --profile full --output-dir target/self-python --format json` passed all seven checks with no violations or incomplete results. Evidence is `target/self-python/run-G6uYxT/report.json`, bound to `sha256:1a9d0eb55da30283d245d5b89fc786030c1d10060b35aaf5611d252fdb10c6dc` (commit `a6e5bd9` plus this implementation, before the final README and evidence notes). `cargo package --locked --offline --allow-dirty` passed archive compilation verification with 117 files; output is `target/verification-python-package.txt`. The new revision still requires its own remote CI confirmation.

## Python CI follow-up

[CI run 34305994383](https://github.com/coolplayagent/qualitygate-cli/actions/runs/34305994383), for `c69aad55b21f0b3c211cc92e81dd2b441a3c7307`, completed successfully in all 17 jobs, including real Python/Maven checks, native Windows/macOS, Miri, AddressSanitizer, coverage and packaging.

## Architecture and documentation harness

The architecture harness now follows declared production modules and extracts a syntax-based owner graph. Grouped/aliased imports, re-exports, relative paths, inline modules, macro arguments and production configuration branches participate; cycle and forbidden-direction fixtures establish failure behavior. The gate exposed direct configuration replacement in the CLI, which now delegates to the configuration owner while retaining atomic writes and permissions. Source digests and file/line edge evidence are retained and uploaded by CI. See [the executable ownership contract](architecture.md#allowed-module-dependencies).

The documentation harness now parses CommonMark and checks local Markdown anchors, rendered/duplicate/Chinese headings, reference links, escaped paths and real code-fence closure. Examples inside code do not become links. Deliberately broken fixtures cover missing anchors/files/references, traversal and malformed escapes. These changes strengthen the Rust-only reference-equivalent quality gates.

The revision passed **144 ordinary tests** (89 library, nine core CLI, 14 custom-rule CLI, 14 execution CLI, seven MR CLI, ten quality and one benchmark). Formatting, compilation and Clippy with warnings denied passed. Final full coverage passed at **92.64% lines** (7,054 lines, 519 missed); output is `target/verification-harness-coverage-final.txt`. This includes the parser-offset regression rejecting indented pseudo-closing fences while accepting fences inside lists and blockquotes. The production graph contains 60 source files and 27 distinct permitted owner directions, with no cycles or boundary violations. The three Maven and one Python live tests remain separate CI jobs; the previous revision's all-17-job result does not establish this revision's CI status.

Self-hosted `target/debug/qualitygate check --worktree --profile full --output-dir target/self-harness --format json` passed all seven checks, with no violations or incomplete results. Evidence is `target/self-harness/run-xdIKXK/report.json`, bound to `sha256:0d4047838059005dbde2223006355f98e4328b1f4d544989ca45f57e145fc183` (commit `c69aad5` plus this implementation). That self-hosted checkpoint preceded the final fence-offset regression and this evidence note; the final full coverage run above validates the fence change. `cargo package --locked --offline --allow-dirty` passed archive compilation verification with 121 files (`target/verification-harness-package-final.txt`).

## Architecture CI follow-up

[CI run 34307533736](https://github.com/coolplayagent/qualitygate-cli/actions/runs/34307533736), for `d616d8be6e00944dcc40bb4a100d2bdc03edb291`, completed successfully in all 17 jobs. Its non-expired `architecture` and `coverage` artifacts were confirmed through the run's artifact API.

## Initialization discovery

The Rust initializer discovers nested manifests and languages, respects bounded local ignore rules, reports capabilities/gaps, and suggests commands with version probes. `--with-checks` opts a new candidate into suggestions with complete report configuration; existing policies remain byte-for-byte intact. Unmapped test commands remain explicit suggestions. [Initialization](init.md) documents exact boundaries.

The same syntax descriptor drives discovery and runtime DSL checks. Unsupported Shell imports/test methods block explicit language contracts without matches and inferred Shell files. Fixtures cover mixed roots, ignored/vendor files, nested overrides, duplicate pytest suggestions, malformed/dynamic/unsupported metadata, manager conflicts, symlinks, size/depth limits, selected paths and repeated initialization. A temporary Cargo project verifies actual assertion failure, repair and zero-test rejection; it does not replace the real-repository pilot.

The final revision passed **153 ordinary tests** (94 library, nine core CLI, 14 custom-rule CLI, 14 execution CLI, four initialization CLI, seven MR CLI, ten quality and one benchmark). Formatting, compilation and Clippy with warnings denied passed. Full coverage passed at **93.12% lines** (7,647 lines, 526 missed); output is `target/verification-discovery-coverage-final.txt`. The four initialization CLI tests, including the real Cargo repair sequence and linked-worktree regression, passed together under coverage in 40.73 seconds. Three Maven and one Python live tests remain separate CI gates.

Running `target/debug/qualitygate init --with-checks --format json` on this repository returned `created: false` and `with_checks_applied: false`, preserving the existing policy. Its output identified the root Cargo project and 128 visible files, suggested Cargo check/test commands, and disclosed project-semantic and provenance gaps (`target/discovery-self.json`). The production architecture graph covers 66 source files and 27 owner directions without violations.

Self-hosted `target/debug/qualitygate check --worktree --profile full --output-dir target/self-discovery --format json` passed all seven checks, with no violations or incomplete results. Evidence is `target/self-discovery/run-MCUVws/report.json`, bound to `sha256:2a178c0129927b2582e6a9e7b578f762a2c83c3be91181a5e72d851c703d9074` (commit `d616d8b` plus the implementation). This checkpoint preceded the linked-worktree fix and final evidence notes. `cargo package --locked --offline --allow-dirty` passed archive compilation verification with 130 files (`target/verification-discovery-package.txt`). This revision still requires its own remote CI confirmation.

A final linked-worktree regression uses a real temporary Git worktree to verify candidate creation, language discovery, unchanged shared metadata and independent policy location. Discovery excludes the `.git` metadata file as well as directories, avoiding a reserved-path failure in linked worktrees.

## Manual acceptance implementation

[CI run 34424992409](https://github.com/coolplayagent/qualitygate-cli/actions/runs/34424992409), for `5077f62e448b5b3dab6274acc649efebdc592a5d`, completed successfully in all 17 jobs, including native platforms, live Maven/Python, Miri, ASan, coverage and packaging.

Manual checks now verify external Ed25519 DSSE decisions using a caller-controlled trust store. Records bind repository identity, the complete snapshot/comparison, policy and task digests, check ID and acceptance ID. Signer scopes, expiration and revocation are enforced; valid rejection is a violation, while unverified evidence remains incomplete. Bounded external inputs are captured before commands and revalidated before publication. Original envelope/trust artifacts remain reviewable. [The protocol](manual-acceptance.md) documents the producer and trust boundaries.

Rust fixtures cover authenticated approval/rejection, dependent execution, byte tampering, unsigned payloads, foreign subjects, expiry, revocation, explicit repository authorization, malformed/oversized/symlink inputs, and code-change/reapproval. Temporary Rust commands prove that changing trust inputs or reaching an approval's expiry during later execution invalidates the result.

The revision passed **166 ordinary tests** (101 library, nine core CLI, 14 custom-rule CLI, 14 execution CLI, four initialization CLI, six manual acceptance CLI, seven MR CLI, ten quality and one benchmark). Formatting, compilation and Clippy with warnings denied passed. Final full coverage passed at **93.45% lines** (8,185 lines, 536 missed); output is `target/verification-manual-coverage-final.txt`. The six manual acceptance scenarios passed together under coverage in 9.44 seconds, including expiration at the completion boundary. The production ownership graph covers 70 source files with no violations. Three Maven and one Python live tests remain separate CI gates; this revision still requires its own remote confirmation.

Self-hosted `target/debug/qualitygate check --worktree --profile full --output-dir target/self-manual --format json` passed all seven checks, with no violations or incomplete results. Evidence is `target/self-manual/run-JEo8Ww/report.json`, bound to `sha256:c8f619128de7abe9eae941897065e82c164793e1b8c9990900058cd0052eba75` (commit `5077f62` plus this implementation, before this evidence note). `cargo package --locked --offline --allow-dirty` passed archive compilation verification with 138 files; output is `target/verification-manual-package.txt`.

## Agent-run provenance implementation

Agent-run provenance is documented in [provenance.md](provenance.md). It authenticates complete file transformations and derives test participation independently of declaration presence. Real harness deployment must establish trustworthy observation and actor identity; the CLI verifies signed transformations and entity matching.

[CI run 34448756749](https://github.com/coolplayagent/qualitygate-cli/actions/runs/34448756749), for `7b0efe228ab3f2e9d91a96ca242a0fe6f4c2fc7c`, completed successfully in all 17 jobs. That result covers the manual-acceptance revision; this provenance revision requires its own verification.

The provenance revision passed **176 ordinary tests** (105 library, nine core CLI, 14 custom-rule CLI, 14 execution CLI, four initialization CLI, six manual acceptance CLI, seven MR CLI, six provenance CLI, ten quality and one benchmark). Formatting, compilation and Clippy with warnings denied passed. Full coverage passed at **93.72% lines** (8,789 lines, 552 missed); output is `target/verification-provenance-coverage.txt`. All six provenance CLI tests passed together under coverage in 10.42 seconds, including record mutation and expiry during later command execution. The production ownership graph covers 74 source files with no violations. Three Maven and one Python live tests remain separate CI gates.

Self-hosted `target/debug/qualitygate check --worktree --profile full --output-dir target/self-provenance --format json` passed all seven checks, with no violations or incomplete results. Evidence is `target/self-provenance/run-yjw0VZ/report.json`, bound to `sha256:a7a57844fbf896da415ecf1b01889ce241592a48007b9e7416ab17087159a640` (commit `7b0efe2` plus this implementation, before this evidence note). `cargo package --locked --offline --allow-dirty` passed archive compilation verification with 145 files; output is `target/verification-provenance-package.txt`. Remote native-platform, Miri, ASan and live-ecosystem checks still require this revision's own CI result.

## Git trailer association implementation

[Git trailer association](git-trailers.md) now reads bounded base/head ancestry, associates actual test entities with their last byte-changing commits, and distinguishes uncommitted changes. All merge parents participate in association. Rust fixtures cover copy/move behavior, unrelated trailers, conflict resolution, shallow history, replacement objects, retained dependencies and signed AI-only scope.

[CI run 34548478003](https://github.com/coolplayagent/qualitygate-cli/actions/runs/34548478003), for `c5e801ada6a0f5bf179c6e425b38f8184f6cd202`, completed successfully in all 17 jobs. That result verifies the signed-provenance revision; this Git trailer revision needs its own verification.

The revision passed **187 ordinary tests** (109 library, nine core CLI, 14 custom-rule CLI, 14 execution CLI, six Git trailer CLI, four initialization CLI, six manual acceptance CLI, seven MR CLI, seven provenance CLI, ten quality and one benchmark). Formatting, compilation and Clippy with warnings denied passed. Final full coverage passed at **93.85% lines** (9,259 lines, 569 missed); output is `target/verification-git-trailers-coverage-final.txt`. All six Git trailer CLI fixtures passed under coverage in 0.88 seconds. The final run includes the merge-parent budget check; the production ownership graph covers 78 source files with no violations. Three Maven and one Python live tests remain separate CI gates.

Self-hosted `target/debug/qualitygate check --worktree --profile full --output-dir target/self-git-trailers --format json` passed all seven checks, with no violations or incomplete results. Evidence is `target/self-git-trailers/run-ttgkKJ/report.json`, bound to `sha256:4cbb111c4a20909fe80818b4a8bcb9b4cb9494e20e9ee5555705f1586f5c94cd` (commit `c5e801a` plus this implementation, before this evidence note). `cargo package --locked --offline --allow-dirty` passed archive compilation verification with 152 files; output is `target/verification-git-trailers-package.txt`. Remote native-platform, Miri, ASan and live-ecosystem checks require this revision's own CI result.

## Interface compatibility verification

[Java interface compatibility](compatibility.md) now builds the same policy command on both selected snapshots, captures digest-bound API/classpath JARs and executes japicmp 0.26.2. A complete private/synthetic inventory is checked first; a second pass compares public/protected APIs against the derived class inventory. This avoids treating private method removal as an API violation while retaining completeness checks. Internal build/inventory evidence cannot collide with authored check names. Build/analyzer timeouts preserve their execution status, and failures retain artifacts.

The final production revision passed **201 ordinary tests** (114 library, ten core CLI, eight compatibility CLI, 14 custom-rule CLI, 14 execution CLI, six Git trailer CLI, four initialization CLI, six manual acceptance CLI, seven MR CLI, seven provenance CLI, ten quality and one benchmark). Formatting, compilation and Clippy with warnings denied passed; logs use the `target/verification-compatibility-api-` prefix. Full `cargo llvm-cov --all-targets --all-features --fail-under-lines 90` passed at **94.12% lines** (10,019 lines, 589 missed); output is `target/verification-compatibility-api-coverage.txt`. The coverage run includes the final fixture that separately asserts incomplete/timed-out API execution after a completed inventory pass. The production architecture graph covers 82 files with no violations and retains source digests and reference locations in `target/architecture/report.json`.

The live JDK 21.0.11/japicmp 0.26.2 fixture passed in **100.87 seconds** (`target/verification-compatibility-api-live.txt`). It checks actual public method deletion/repair, private method removal, private constructor addition, source-only generic changes, missing classpaths, multiple API JARs and dependency API repair. Five live ecosystem tests remain separate from the ordinary test count: this compatibility fixture, three Maven tests and one Python test. The new `java-compatibility` CI job executes the fixture explicitly; the current 18-job workflow still requires this revision's own remote results, including native Windows/macOS, Miri and ASan.

The additional `tests/cli.rs::trusted_policy_preserves_severity_task_items_and_verification_inputs` scenario verifies that lowering required-rule severity, removing one task acceptance item, editing/deleting a verification program and adding a matched verification asset cannot pass the original trusted policy. Unchanged inputs pass before and after these temporary-repository mutations.

Self-hosted `target/debug/qualitygate check --worktree --profile full --output-dir target/self-compatibility --format json` passed all seven checks with no violations or incomplete results, including 114 unit tests, 76 integration tests and ten quality tests. Evidence is `target/self-compatibility/run-NUq8Mp/report.json`, bound to `sha256:f7395aaad4f941506127dfdf9757ba90a8be6cc62ced85ab8d492fdd8fee6b8b` (commit `4df4c1f` plus this implementation, before this evidence note). `cargo package --locked --offline --allow-dirty` passed archive compilation verification with 161 files; output is `target/verification-compatibility-api-package.txt`.

The [pilot protocol](pilot.md) specifies baseline attribution, sample classification, review denominators, repair budgets and immutable case records. A team-selected real repository, observation period, thresholds and measured acceptance records are still absent; controlled fixtures and the self-hosted run do not satisfy §9.2 or establish completion of the entire goal.

## SARIF completeness and incremental locations

[CI run 34551181399](https://github.com/coolplayagent/qualitygate-cli/actions/runs/34551181399), for `4df4c1f70db1e8176294873832eaac18dec4e6e2`, completed successfully in all 17 jobs. This confirms the Git trailer revision, including native Windows/macOS, Miri, ASan and existing Maven/Python jobs. The later Java compatibility and SARIF changes require their own CI results.

The [SARIF supported profile](sarif.md) resolves indexed rules/artifacts/logical locations, chained URI bases, escaped paths and catalog messages. It rejects failed/malformed invocations, unresolved outcomes, externalized results and unbound pre-baselined reports. Result kinds and reported suppressions cannot silently reduce the configured gate. Raw artifacts and reported producer/count metadata remain reviewable.

Requirements §3.2, §3.5, §6.3 and §8 are covered by `adapters/reports/sarif_tests.rs`, `application/report_gate_tests.rs` and the three `tests/sarif.rs` CLI scenarios. The integration suite proves independent baseline/current absolute path mapping, historical warning retention through line moves, secondary-location range selection, stable feedback IDs after location reordering, changed producer identity, out-of-source coordinates and raw evidence after incomplete analysis. A discovered feedback-ID defect was fixed: the selected display location no longer changes the identity of a multi-location finding. Legacy generic single-location fingerprints are preserved.

`tests/sarif_live.rs` executes real pinned Clippy and clippy-sarif 0.8.0 through a Rust fixture driver. Its final baseline/new-warning/repair/compiler-error loop passed in **19.24 seconds** (`target/verification-sarif-live-final.txt`). The independent `sarif-project` CI job explicitly runs this external-tool fixture; the regular integration job and local policy include the deterministic SARIF CLI suite. The workflow now contains 19 jobs.

The final SARIF revision passed **217 ordinary tests** (127 library, ten core CLI, eight compatibility CLI, 14 custom-rule CLI, 14 execution CLI, six Git trailer CLI, four initialization CLI, six manual acceptance CLI, seven MR CLI, seven provenance CLI, three SARIF CLI, ten quality and one benchmark). All required stable commands passed: formatting, compilation, Clippy with warnings denied and `cargo test --all-targets --all-features`. Logs use the `target/verification-sarif-*-final.txt` names. Full `cargo llvm-cov --all-targets --all-features --fail-under-lines 90` passed at **94.45% lines** (10,590 lines, 588 missed), recorded in `target/verification-sarif-coverage-final.txt`. This includes cumulative expansion/reference budgets, repeated producer/rule name amplification, raw URI controls/whitespace and encoded backslashes before platform conversion. The production architecture graph covers 83 source files with no violations and retains source digests and file/line references. Six external-tool tests remain separate: three Maven, one Python, one Java compatibility and one SARIF acceptance fixture.

Self-hosted `target/debug/qualitygate check --worktree --profile full --output-dir target/self-sarif-final --format json` passed all seven checks, including 127 unit tests, 79 integration tests and ten quality tests, without violations or incomplete results. Evidence is `target/self-sarif-final/run-l4xJXG/report.json`, bound to `sha256:d73631a2568730a6448b10fdb13672eeb9a291dcb419e66a76479d511b24b754` (commit `4df4c1f` plus this implementation, before this evidence note). `cargo package --locked --offline --allow-dirty` passed archive compilation verification with 166 files; output is `target/verification-sarif-package-final.txt`. The source revision still needs its own remote 19-job CI confirmation. Real-repository pilot selection, team thresholds and measured outcomes remain pending.

## Main synchronization and coverage interoperability

On 2026-09-12, local `main` fast-forwarded from `4df4c1f` to remote `7d6fb2ad2565d0f0efaedddbfbaa33222fe03b7e`. Remote lifecycle/standards contracts, catalog validation, maps and documentation were preserved while restoring the later local Java API visibility, SARIF and coverage work. The pre-update working tree and verified SARIF package remain backed up; synchronization did not publish the unfinished coverage implementation.

[CI run 34688637943](https://github.com/coolplayagent/qualitygate-cli/actions/runs/34688637943), for `7d6fb2a`, completed successfully in all 18 jobs, including native Windows/macOS, Miri, ASan and Java compatibility. This confirms the fetched remote revision. The later local API visibility, SARIF and coverage changes still require their own remote results; the revised workflow has 20 jobs.

The [coverage producer profiles](coverage.md) add native coverage.py JSON, recognized inert XML declarations, JaCoCo rollup validation, exact Cobertura roots and bounded normalized/mapped path expansion. Zero-only Cobertura and LCOV counters no longer claim branch measurement. Native JSON records explicit branch measurement and preserves snapshot-validated exclusions. The coverage.py empty-module line-zero sentinel is handled only with consistent zero-statement evidence.

Requirements §3.2, §3.5, §5.2, §6.3, §6.6 and §8 map to `adapters/reports/coverage_tests.rs`, `application/report_gate_tests.rs`, `tests/coverage.rs`, `tests/coverage_live.rs` and `tests/coverage_llvm_live.rs`. Rust tests cover omitted sources, corrupt summaries, duplicate identities, source-root ambiguity, staged selection, trusted policy changes, timeouts, input mutation and retained raw artifacts. Real JDK 21/JaCoCo 0.8.15 and Python 3.12/coverage.py 7.10.7/pytest 8.4.2 tests passed together in 27.06 seconds (`target/verification-coverage-live-native.txt`). The pinned Rust/LLVM LCOV fixture passed separately in 3.24 seconds (`target/verification-coverage-live-llvm.txt`). These are controlled producer fixtures, not §9.2 pilot measurements.

The final production revision passed **231 ordinary tests**: 139 library, ten core CLI, eight compatibility CLI, two coverage CLI, 14 custom-rule CLI, 14 execution CLI, six Git trailer CLI, four initialization CLI, six manual acceptance CLI, seven MR CLI, seven provenance CLI, three SARIF CLI, ten quality and one benchmark. All required stable commands passed: formatting, compilation, Clippy with warnings denied and `cargo test --all-targets --all-features`. Logs are `target/verification-coverage-{fmt,check,clippy,all}-final.txt`. Full `cargo llvm-cov --all-targets --all-features --fail-under-lines 90` passed at **94.64% Rust lines** (11,272 lines, 604 missed), recorded in `target/verification-coverage-lines-final.txt`. The production architecture graph covers 86 files with no violations and retains source digests and file/line references in `target/architecture/report.json`.

The final combined producer run passed the JaCoCo/coverage.py pair in 43.01 seconds and LLVM LCOV in 6.06 seconds (`target/verification-coverage-producers-final.txt`), including the mapped-path budget and zero-only LCOV correction. Nine live tests remain separate from the ordinary suite: three Maven, one Python dependency, one Java compatibility, one SARIF and these three coverage producer tests. The 20-job workflow includes dedicated SARIF and coverage-producer jobs; this uncommitted revision has no remote CI result yet.

Self-hosted `target/debug/qualitygate check --worktree --profile full --output-dir target/self-coverage-final --format json` passed all seven checks without violations or incomplete results, including 139 unit tests, 81 integration tests and ten quality tests. Evidence is `target/self-coverage-final/run-4KIxFX/report.json`, bound to `sha256:4b2db67e6283b463a1f17b895bd7430c6f9146b915a22512c666becc78227820` (commit `7d6fb2a` plus this implementation, before this evidence note). `cargo package --locked --offline --allow-dirty` passed archive compilation verification with 213 packaged files (`target/verification-coverage-package-final.txt`). Full requirements acceptance and the team-selected real-repository pilot remain pending.

## Normative source review verification

[Source review records](source-reviews.md) implement the §6.6 requirement that refreshing a section hash alone cannot complete a normative review. Records bind the full rule definition, effective settings and engine version, and retain reviewer identity assertions, an external reference, a conclusion and rationale in the selected policy. Missing or stale records leave required rules incomplete, including rules with no matching entities. Rule evidence retains binding status and the policy trust label. These records derive their authority from the caller's selected policy workflow; they do not authenticate a reviewer or replace signed manual acceptance.

`tests/source_reviews.rs` covers missing records, hash-only updates, explicit review refresh, effective-setting changes, malformed records, optional completeness, staged document isolation and caller-selected policy versions. Custom-rule, Git trailer, signed-provenance, Maven and Python fixtures now explicitly record their fixture-policy reviews. Pure domain and configuration tests cover record constraints and binding changes. Strict YAML loading rejects duplicate mapping keys before typed deserialization, preventing later records, rule settings or parameters from silently replacing earlier values. The initial duplicate-key regression failed before the parser fix (`target/verification-source-review-duplicate.txt`); the final suite passes without weakening that assertion. CommonMark source parsing excludes headings inside code, HTML comments, lists and quotations while retaining exact section bytes and the documented raw ATX title profile.

The revision passed **238 ordinary tests** (143 library, 84 CLI integration, ten quality and one benchmark); nine live-tool tests remain separate. All required stable commands passed: `cargo fmt --all -- --check`, `cargo check --all-targets --all-features`, `cargo clippy --all-targets --all-features -- -D warnings` and `cargo test --all-targets --all-features`. Logs use `target/verification-source-review-{fmt,check,clippy,all}-final.txt`. Full `cargo llvm-cov --all-targets --all-features --fail-under-lines 90` passed at **94.72% Rust line coverage** (11,450 lines, 605 missed; `target/verification-source-review-coverage-final.txt`). The architecture gate covers 88 production source files without violations, retaining source digests and file/line evidence in `target/architecture/report.json`.

The changed real Maven dependency-pairing fixture passed in **157.90 seconds** with Maven 3.9.16/JDK 21.0.11 (`target/verification-source-review-maven-final.txt`). The changed real Python fixture passed in **40.87 seconds** (`target/verification-source-review-python-final.txt`). These isolated Git fixtures establish composition with source reviews, not human approval or real-repository pilot results. Native Windows/macOS, Miri and ASan still require this revision's own CI confirmation; the successful 18-job run for `7d6fb2a` predates these local changes.

Self-hosted `target/debug/qualitygate check --worktree --profile full --output-dir target/self-source-review-final --format json` passed all seven checks without violations or incomplete results. Evidence is `target/self-source-review-final/run-6GRqyc/report.json`, bound to `sha256:ae9faa94be0dea5ce0f790f34e83f299b8fb7246993921d99b9b54d85db929f9` (commit `7d6fb2a` plus this implementation). `cargo package --locked --offline --allow-dirty` passed archive compilation verification with 218 files (`target/verification-source-review-package-final.txt`). Both checkpoints precede this final evidence note; the documentation gate was rerun after adding it. Full requirements acceptance and the team-selected real-repository pilot remain pending.

## Selected task policy verification

[Task planning](tasks.md) now selects configuration, custom definitions and the requested task from one resolved policy commit. Previously, a changed candidate task correctly blocked the gate but still supplied its shortened acceptance plan and task digest. The new regression failed on the missing original acceptance item before the implementation changed (`target/verification-policy-task-regression.txt`). Candidate task deletion/corruption and candidate configuration removal now retain the complete selected plan. File-mode changes participate in policy comparison, and overlapping verification-asset patterns produce one sorted entry per changed path. Parsing and plan construction run on a blocking worker.

Reports add the selected policy commit, task path and task identity, retain observable acceptance descriptions, and expose each selected configured command's full acceptance definition even when it cannot run. Definitions remain distinct from actual execution evidence. Diagnostic rechecks pin the selected policy commit; a temporary Git branch-movement fixture executes the recheck and proves that it cannot silently adopt a weakened replacement policy. Original error diagnostics remain visible alongside incomplete policy validation. Optional report fields are additive; successful task digest and signed-subject semantics remain unchanged.

The reusable `Plan::build` API also validates directly constructed task contracts. A separate unit regression first proved that an unsupported task schema could bypass YAML validation (`target/verification-policy-task-core-regression.txt`). Shared semantic validation now rejects unsupported versions, empty task IDs/inventories, invalid or duplicate acceptance IDs and empty descriptions for both profiles. This protects core-library callers as well as the CLI; it does not require serializing their objects through YAML.

The final revision passed **242 ordinary tests** (144 library, 87 CLI integration, ten quality and one benchmark); nine live-tool tests remain separate. Required formatting, compilation, Clippy with warnings denied and all-target/all-feature tests passed (`target/verification-policy-task-core-{fmt,check,clippy,all}-final.txt`). Full `cargo llvm-cov --all-targets --all-features --fail-under-lines 90` passed at **94.79% Rust line coverage** (11,526 lines, 601 missed; `target/verification-policy-task-core-coverage-final.txt`). The unchanged architecture gate covers 88 production source files without violations and retains source digests and file/line edges in `target/architecture/report.json`. The policy fixtures run through both CI and the self-hosted integration check; the signed manual acceptance and source-review suites pass with the new selection behavior. The new documentation's initially broken local anchor was repaired and the unchanged documentation gate passed in the final suite.

Self-hosted `target/debug/qualitygate check --worktree --profile full --output-dir target/self-policy-task-core-final --format json` passed all seven checks. Evidence is `target/self-policy-task-core-final/run-UqMzgi/report.json`, bound to `sha256:9d04d8a84f145bf7d6734831273ed4c5759752f2fe082da561020b25c637da68` (commit `7d6fb2a` plus this implementation, including the reusable-core validation). `cargo package --locked --offline --allow-dirty` passed archive compilation verification with 220 files (`target/verification-policy-task-core-package-final.txt`). Both checkpoints precede this final evidence note, after which the documentation gate was rerun. The current reference workflows were rechecked read-only: the optional manual Qodana workflow and stable/Miri/ASan/coverage contracts remain aligned within this CLI's documented scope. This revision still needs its own native-platform, Miri and ASan CI results; full requirements acceptance and the team-selected real-repository pilot remain pending.

## Remaining implementation and audit work

- Complete further semantic capabilities used by custom DSL assertions; audit signed provenance deployment and protocol migration against real second-ecosystem evidence.
- Audit Maven pairing, bytecode usage and module dependency directions against the required supported scope; additional package/access boundaries and ecosystem expansion need explicit adapter capabilities. Java interface compatibility now has a paired-build implementation and dedicated verification.
- Audit Git trailer association and history budgets on real repositories; audit agent-run observation, ambiguous entity histories and deployment of the manual acceptance trust boundary with real external review services.
- Audit live-provider MR compatibility beyond the controlled HTTP/Git fixtures; retain bounded network access, immutable comparisons and local checkout preservation.
- Audit trusted strategy deployment and verification asset coverage. Source hashes use unambiguous CommonMark sections and effective mappings require bound review records. Configuration, custom rules and tasks use one resolved policy version, and rechecks retain its commit. Project-specific verification inputs still require explicit inventory; real team approval and protected-reference selection remain the caller's workflow responsibility.
- Audit coverage configuration and supported producer profiles on the selected pilot repository. JaCoCo, coverage.py XML/JSON and Rust/LLVM LCOV now have real-tool fixtures; ambiguous zero counters, omitted sources, source/debug gaps and contradictory summaries stay incomplete. The SARIF profile has bounded indexed resolution, actual Clippy interoperability and paired-baseline validation; external bundles and other extensions remain explicitly unsupported.
- Complete entity-change auditing for changed names, cross-file comments and declaration retention. Multiset tests now prove moves/copies, Java overloads and annotation removal; mixed-language/module boundaries still require project facts.
- Complete the remaining asynchronous I/O/CPU and environment audit. Input scans, executable hashing and report parsing/filtering now use workers; per-command/baseline guards and actual version probes have regression coverage. Audit filesystem race limits and dynamically generated inputs without presenting integrity checks as an OS sandbox.
- Extend the architecture/documentation gates if new source-generation forms or document formats are introduced; the current top-level owner graph and local Markdown/YAML contracts now have executable checks.
- Maintain the unchanged ≥90% coverage threshold as implementation expands; run self-hosted quality checks, packaging, Miri, ASan and native-platform verification where available.
- Run and record a reproducible real-repository pilot and its measurement protocol. Do not substitute synthetic successes for unmeasured human-review savings.
