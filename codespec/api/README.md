# Api

This directory is governed by `codespec/codespec-map.yaml`. Update its map entry through `relay-knowledge map directory` and keep reviewed source material within the declared content scope.

[Rule management](../../docs/rule-management.md) documents progressive reads
and draft compatibility. [Policy evolution](../../docs/policy-evolution.md)
and [protected validation](../../docs/policy-validation.md) document the strict
archive, suite, trust, DSSE approval, rollback and effectiveness contracts.
