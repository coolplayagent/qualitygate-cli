schema_version: 1
task_id: rust-bug-fix-v1
acceptance:
  - id: regression-behavior
    description: The independently reviewed regression cases for the reported bug pass.
    required: true
    severity: error
    verification:
      kind: command
      check_id: rust-regression
      argv: [cargo, test, --locked, --offline, --test, regression]
      timeout_seconds: 300
      tools:
        - id: cargo
          argv: [cargo, --version]
          inputs: [Cargo.toml, Cargo.lock]
        - id: rustc
          argv: [rustc, --version]
