id: diff-size
version: 1
description: Report changes exceeding the configured added-line budget
implementation: diff-size
language: []
requires_capabilities: ["files"]
defaults:
  required: true
  severity: warning
  parameters: {}
standard_refs:
  - {source_id: google-code-review, controls: [focused-change, code-health]}
  - {source_id: cloudflare-codex, controls: [reviewability, merge-gate]}
lifecycle_inputs: [focused-change-review]
