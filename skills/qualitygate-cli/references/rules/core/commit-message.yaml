id: commit-message
version: 1
description: Validate new commit subjects against the team convention
implementation: commit-message
language: []
requires_capabilities: ["commits"]
defaults:
  required: true
  severity: error
  parameters: {}
standard_refs:
  - {source_id: aws-code-review, controls: [change-review, completion-criteria]}
  - {source_id: cloudflare-codex, controls: [governed-standards, merge-gate]}
lifecycle_inputs: [change-subject-traceability]
